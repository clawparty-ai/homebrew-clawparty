# Homebrew Tap for ClawParty

Install with:

```bash
brew tap clawparty-ai/clawparty
brew install clawparty-ai/clawparty/clawparty
```

After install, both commands are available:

- `clawparty`
- `ztm`
